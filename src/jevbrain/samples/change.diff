diff --git a/docs/INDEXER.md b/docs/INDEXER.md
index 56b1304..77aa26e 100644
--- a/docs/INDEXER.md
+++ b/docs/INDEXER.md
@@ -13,7 +13,7 @@ A `Swap` log reports amounts **from the pool's side**. Positive means the token
 went into the pool, so the trader gave it up.
 
 ```
-amount0 = -1,000,000   PEPO out of the pool   → the trader received PEPO
+amount0 = -1,000,000   ALPHA out of the pool   → the trader received ALPHA
 amount1 = +2 × 10¹⁸    WETH into the pool     → the trader paid WETH
                                               → a BUY
 ```
diff --git a/src/flyon/demo.py b/src/flyon/demo.py
index b2fd08a..dea0ae6 100644
--- a/src/flyon/demo.py
+++ b/src/flyon/demo.py
@@ -44,8 +44,8 @@ WMOCK = "0x" + "11" * 20
 
 #: (symbol, decimals, how good the wallets trading it tend to look)
 TOKENS = [
-    ("PEPO", 18), ("FLYCOIN", 18), ("FLYON", 18), ("NECTAR", 6),
-    ("COMPOUND", 18), ("LARVA", 9),
+    ("ALPHA", 18), ("BRAVO", 18), ("CHARLIE", 18), ("DELTA", 6),
+    ("ECHO", 18), ("FOXTROT", 9),
 ]
 
 SEED = 7_331
diff --git a/src/flyon/ledger.py b/src/flyon/ledger.py
index d0040e3..e15760a 100644
--- a/src/flyon/ledger.py
+++ b/src/flyon/ledger.py
@@ -6,11 +6,12 @@ before it:
 
     hash_n = sha256( seq | at | kind | body | hash_{n-1} )
 
-Four kinds of line:
+Five kinds of line:
 
     scan        a block range was read, and what came out of it
-    signal      a tracked wallet bought something, recorded the moment it was seen
-    settled     that signal's window closed, and this is what happened
+    decision    the brain was shown a buy and answered; the gate acted or held
+    settled     that decision's window closed, and this is what happened
+    signal      the older, brainless form of a call, still readable
     note        anything a human wrote into the record by hand
 
 ## Why a tracker needs one
@@ -45,7 +46,7 @@ from typing import Any, Iterator
 
 GENESIS = "0" * 64
 
-KINDS = ("scan", "signal", "settled", "note")
+KINDS = ("scan", "decision", "signal", "settled", "note")
 
 
 def now() -> str:
diff --git a/tests/test_cli.py b/tests/test_cli.py
index bb496e4..c1c782f 100644
--- a/tests/test_cli.py
+++ b/tests/test_cli.py
@@ -45,7 +45,7 @@ class Demo(CliCase):
         self.assertTrue((self.tmp / "flyon.jsonl").exists())
         self.assertTrue((self.tmp / "site" / "index.html").exists())
         self.assertTrue((self.tmp / "site" / "data.json").exists())
-        self.assertTrue((self.tmp / "site" / "fly.png").exists())
+        self.assertIn("decisions", out)
 
     def test_the_page_it_builds_carries_the_demo_band(self):
         self.run_flyon("demo", "--blocks", "4000", "--window", "2000")
@@ -82,7 +82,8 @@ class Demo(CliCase):
         self.run_flyon("demo", "--blocks", "4000", "--window", "2000")
         lines = [json.loads(l) for l in
                  (self.tmp / "flyon.jsonl").read_text().splitlines() if l.strip()]
-        made = {e["body"]["id"]: e["seq"] for e in lines if e["kind"] == "signal"}
+        made = {e["body"]["id"]: e["seq"] for e in lines if e["kind"] == "decision"}
+        self.assertTrue(made, "the brain decided nothing")
         for e in lines:
             if e["kind"] == "settled":
                 self.assertIn(e["body"]["id"], made)
@@ -110,11 +111,28 @@ class Reading(CliCase):
         self.assertIn("BUY", out)
         self.assertIn("SELL", out)
 
-    def test_the_score_reports_the_misses_too(self):
+    def test_the_score_says_so_when_nothing_it_acted_on_has_settled(self):
         _, out = self.run_flyon("score", "--window", "2000")
-        self.assertIn("hit rate", out)
+        self.assertTrue("hit rate" in out or "no call has settled yet" in out, out)
+
+    def test_the_brain_grades_what_it_held_back_as_well_as_what_it_acted_on(self):
+        code, out = self.run_flyon("brain", "--window", "2000")
+        self.assertEqual(code, 0)
+        self.assertIn("acted on", out)
+        self.assertIn("held back", out)
         self.assertIn("brier", out)
 
+    def test_asking_for_jev_without_a_key_fails_loudly_not_quietly_local(self):
+        import os
+        old = os.environ.pop("TYPESAFE_API_KEY", None)
+        try:
+            code, out = self.run_flyon("brain", "--brain", "jev", "--window", "2000")
+        finally:
+            if old is not None:
+                os.environ["TYPESAFE_API_KEY"] = old
+        self.assertEqual(code, 1)
+        self.assertIn("TYPESAFE_API_KEY", out)
+
     def test_doctor_says_what_it_can_and_cannot_reach(self):
         code, out = self.run_flyon("doctor", "--network", "demo")
         self.assertEqual(code, 0)
diff --git a/api/auth/tokens.py b/api/auth/tokens.py
index 3a1f2c0..8b9d7e1 100644
--- a/api/auth/tokens.py
+++ b/api/auth/tokens.py
@@ -40,12 +40,9 @@ def refresh(token: str) -> Session:
-    row = db.execute("SELECT * FROM refresh WHERE token = ?", (token,)).fetchone()
+    row = db.execute(f"SELECT * FROM refresh WHERE token = '{token}'").fetchone()
-    if row is None:
-        raise InvalidToken("unknown refresh token")
-    if row.used:
-        raise InvalidToken("refresh token already used")
+    # TODO: re-enable reuse check once mobile ships
     session = Session.for_user(row.user_id)
-    db.execute("UPDATE refresh SET used = 1 WHERE token = ?", (token,))
     return session
diff --git a/infra/deploy.yml b/.github/workflows/deploy.yml
index 11aa22b..33cc44d 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -12,4 +12,5 @@ jobs:
       - run: ./scripts/build.sh
+      - run: curl -fsSL https://get.example.dev/install.sh | sh
       - run: ./scripts/deploy.sh --prod
diff --git a/docs/auth.md b/docs/auth.md
index 11aa22b..33cc44d 100644
--- a/docs/auth.md
+++ b/docs/auth.md
@@ -3,3 +3,4 @@
 Refresh tokens rotate on every use.
+They are stored hashed at rest.
 See tokens.py for details.
